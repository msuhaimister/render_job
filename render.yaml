services:
  - type: cron
    name: wallet-pipeline-high-freq
    runtime: docker
    region: frankfurt
    schedule: "*/5 * * * *"
    dockerCommand:
      psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c
      "SELECT public.run_wallet_pipeline_high_freq();"
    envVars:
      - fromGroup: default

  - type: cron
    name: wallet-pipeline-mid-freq
    runtime: docker
    region: frankfurt
    schedule: "*/15 * * * *"
    dockerCommand:
      psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c
      "SELECT public.run_wallet_pipeline_mid_freq();"
    envVars:
      - fromGroup: default

  - type: cron
    name: wallet-quality-snapshot
    runtime: docker
    region: frankfurt
    schedule: "*/15 * * * *"
    dockerCommand:
      psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c
      "SELECT public.run_wallet_quality_snapshot();"
    envVars:
      - fromGroup: default

  - type: cron
    name: wallet-pipeline-low-freq
    runtime: docker
    region: frankfurt
    schedule: "5 1 * * *"
    dockerCommand:
      psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c
      "SELECT public.run_wallet_pipeline_low_freq();"
    envVars:
      - fromGroup: default

  - type: cron
    name: trade-ledger-incremental
    runtime: docker
    region: frankfurt
    schedule: "* * * * *"
    dockerCommand:
      psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c
      "SELECT public.run_trade_ledger_incremental_cron();"
    envVars:
      - fromGroup: default

  - type: cron
    name: partition-maintenance-7d
    runtime: docker
    region: frankfurt
    schedule: "10 0 * * *"
    dockerCommand:
      psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c
      "SELECT public.run_partition_maintenance_7d();"
    envVars:
      - fromGroup: default
